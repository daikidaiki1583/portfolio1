import React, { FC, useContext } from 'react';
import Graph from '../organisms/graph';
import { myContext } from '../../Context';

const Home: FC = () => (
  <>
    <div>home</div>

    {/* <Graph /> */}
  </>
);
export default Home;
